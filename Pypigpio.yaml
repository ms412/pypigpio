app:
  name: pypigpio
  version: "0.01"

gpio:
  CHIP_PATH: /dev/gpiochip0
  CONSUMER: openhab-pigpio-bridge
  HW_REVISION: "0xA02082"

server:
  SERVER_HOST: 0.0.0.0
  SERVER_PORT: 8888

logging:
  LOG_LEVEL: DEBUG
  LOG_METHOD: PRINT
